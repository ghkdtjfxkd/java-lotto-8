> 고민되는 주제에 대해 생각의 흔적을 남겨두는 곳
```
2025-10-23: <주제>
 - 고민 : 
 - 결론 : 
```

2025-10-31: 리포지토리와 DTO
- 고민 : 현재 미션에서 repository는 service를 stateless하게 두기 위한 개념적인 표현으로 사용된다.
  - CQRS 패턴을 도입할까 고민하던 중 너무 과하다는 생각에 단일 repository를 두기로 했는데
  - 이때, 조회의 경우 DTO를 만들어도 되지 않을까? 라는 생각이 들었다.
  - Query Side는 조회만을 담당하며 값을 변경하지 않아야 하는데 그렇다면 불변 dto로 반환해도 이상할게 없지 않을까? 
  - 그럼 repository 가 dto를 생성하면 서비스는? 받은 dto를 그대로 넘기기만? 
  - controller <-> service 계층간 데이터 이동에 dto가 필요하고, dto로 래핑하는게 service의 역할로 아는데 공부가 필요한 듯 싶다.
- 결론 : 

2025-11-01: CQRS 패턴 도입
- 고민 : 각 도메인 엔티티에 대해서 하나의 Repository를 가지고 그 주소값을 서비스에 주입해서 사용한다.
  - 그렇다면 서비스를 Command Side와 Query Side를 분리할 수 있겠거니 생각이 들었다.
  - 하지만 기존 CQRS 패턴은 DB 분리 뿐만 아니라 CDC(Changed Data Capture) 로직이 필요하다.
  - EDA를 도입한다면 비교적 수월했겠지만, 이번 미션에서는 적용하지 않는다.
  - 그렇다면 단일 DB로 사용한다면 서비스의 관심사, 책임 정도만 나눌 수 있을 거 같다.
  - 당장 의미가 있을까????
- 결론 : 시간이 남는다면 리팩토링 해보자.

2025-11-03: 예외 처리에 대한 클래스의 볼륨과 캡슐화
- 고민 : 현재 각 관심사에 맞는 커스텀 예외를 던지게끔 구성했다.
  - package-private 한 예외 타입과 커스텀 예외를 만들었는데, 중복되는 로직이 많았다.
  - 각 상황에 맞는 예외를 발생시키자니 클래스의 볼륨이 너무 커진다.
  - 그렇다고 관심사가 다를 때 캡슐화를 깨야할까??
- 결론 :

2025-11-03: Query Side 의 책임
- 고민 : 나는 이전까지 CQRS 패턴을 도입했을 때 정말 값을 읽기만 해야한다고 생각했었다.
  - 이 때, Query service는 값을 가져와서 다른 도메인과 작업을 하고,
  - 리포지토리에 저장하지 않고 DTO로 프레젠테이션 레이어로 전달한다고 가정했을 때, 
  - 이 작업을 Query side의 책임으로 봐야할까?? 비즈니스 로직이 있으니 Command Side로 봐야할까??
- 결론 : 영속화된 엔티티의 상태를 변경하는건 아니니 Query Side로 봐도 되지 않을까...?